apiVersion: v1
kind: Service
metadata:
  name: {{ include "proxysql-fli.fullname" . }}
  labels:
    {{- include "proxysql-fli.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "proxysql-fli.selectorLabels" . | nindent 4 }}
